import 'package:flutter/material.dart';

Shadow TextShadow() {
    return Shadow(
                    offset: Offset(0, 0.8),
                    blurRadius: 4,
                    color: Colors.black,
                  );
  }
