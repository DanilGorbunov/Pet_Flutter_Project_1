import 'package:flutter/material.dart';

class GradientForTime extends StatelessWidget {
  const GradientForTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
